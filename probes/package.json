{
  "private": true,
  "scripts": {
    "test": "npm run test:node",
    "build:test": "../node_modules/.bin/rollup -c",
    "clean:test": "rm -f test-out.js test-out.js.map",
    "test:node": "../node_modules/.bin/ts-node test/index.ts"
  }
}
