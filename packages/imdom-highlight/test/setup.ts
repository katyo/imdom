import {
    // import basic APIs
    registerLanguages,

    // import preferred languages
    Rust,
    TypeScript,
    Markdown
} from '../src';

// register languages
registerLanguages(
    Rust,
    TypeScript,
    Markdown
);
